package com.androidtechguru.codelab.android.storage

/**
 * ANDROID STORAGE — Interview Prep Topics
 *
 * 1. Room Database: @Entity, @Dao, @Database, @Query, @Insert, @Update, @Delete
 * 2. Room + Coroutines & Flow: suspend DAOs, Flow<List<T>>
 * 3. Room Migrations: manual migration, auto-migration, fallbackToDestructiveMigration
 * 4. Room Relations: @Embedded, @Relation, one-to-many, many-to-many
 * 5. Room Type Converters: @TypeConverter for custom types
 * 6. DataStore Preferences: key-value storage, migration from SharedPreferences
 * 7. Proto DataStore: type-safe schema-based storage
 * 8. SharedPreferences: legacy API, when still appropriate
 * 9. File Storage: internal vs external, scoped storage (Android 10+)
 * 10. Content Providers: basics, when needed
 *
 * Practice: Write examples for each topic below.
 */
