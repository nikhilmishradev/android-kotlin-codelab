package com.androidtechguru.codelab.android.concurrency

/**
 * ANDROID CONCURRENCY — Interview Prep Topics
 *
 * 1. Coroutines in Android: viewModelScope, lifecycleScope, repeatOnLifecycle
 * 2. Main Safety: withContext(Dispatchers.IO), main-safe repository pattern
 * 3. WorkManager: OneTimeWorkRequest, PeriodicWorkRequest, constraints, chaining
 * 4. WorkManager Observing: WorkInfo, LiveData<WorkInfo>, Flow<WorkInfo>
 * 5. Services: Foreground Service, Bound Service — when to use vs WorkManager
 * 6. Broadcast Receivers: static vs dynamic registration, local broadcasts
 * 7. AlarmManager: exact vs inexact alarms, doze mode considerations
 * 8. Thread Safety: Mutex, AtomicReference, synchronized — coroutine-safe patterns
 *
 * Practice: Write examples for each topic below.
 */
